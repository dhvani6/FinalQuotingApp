<%= form_with(model: tire_replacement_front, local: true) do |form| %>
  <% if tire_replacement_front.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tire_replacement_front.errors.count, "error") %> prohibited this tire_replacement_front from being saved:</h2>

      <ul>
      <% tire_replacement_front.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :front_tires %>
    <%= form.text_field :front_tires %>
  </div>

  <div class="field">
    <%= form.label :order_number %>
    <%= form.text_field :order_number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
