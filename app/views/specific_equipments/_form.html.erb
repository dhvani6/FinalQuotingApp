<%= form_with(model: specific_equipment, local: true) do |form| %>
  <% if specific_equipment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(specific_equipment.errors.count, "error") %> prohibited this specific_equipment from being saved:</h2>

      <ul>
      <% specific_equipment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :serial_number %>
    <%= form.text_field :serial_number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
