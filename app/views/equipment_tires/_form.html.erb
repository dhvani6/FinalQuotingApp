<%= form_with(model: equipment_tire, local: true) do |form| %>
  <% if equipment_tire.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(equipment_tire.errors.count, "error") %> prohibited this equipment_tire from being saved:</h2>

      <ul>
      <% equipment_tire.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
