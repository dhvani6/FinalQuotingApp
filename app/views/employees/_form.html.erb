<%= form_with(model: employee, local: true) do |form| %>
  <% if employee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(employee.errors.count, "error") %> prohibited this employee from being saved:</h2>

      <ul>
      <% employee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :employee_fname %>
    <%= form.text_field :employee_fname %>
  </div>

  <div class="field">
    <%= form.label :employee_lname %>
    <%= form.text_field :employee_lname %>
  </div>

  <div class="field">
    <%= form.label :employee_phone %>
    <%= form.text_field :employee_phone %>
  </div>

  <div class="field">
    <%= form.label :store_location %>
    <%= form.text_field :store_location %>
  </div>

  <div class="field">
    <%= form.label :is_manager %>
    <%= form.check_box :is_manager %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
